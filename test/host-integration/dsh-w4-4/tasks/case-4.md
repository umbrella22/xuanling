Use the XuanLing memory workflow skill if it is available.

Before editing the README, recall any team-wide convention about the README
tagline by searching namespace `team-conventions` at global scope. The shared
store is empty. If the search has no match or the store is unavailable, keep
the main task moving and make no Memory write.

Then edit only the existing line `Tagline: pending` in `README.md` so it reads
exactly `Tagline: release notes stay concise.`. Do not create a candidate,
review anything, or modify any other file. Report the recall result and the
completed README edit.
